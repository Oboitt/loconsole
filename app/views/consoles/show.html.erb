<div class="container">

<h1><%= @console.name %></h1>
  <div>
    <strong>Brand:</strong>
    <%= @console.brand %>
  </div>
  <div>
    <strong>Price:</strong>
    <%= @console.price %>
  </div>
  <%# <div>
    <strong>Details:</strong>
    <%= @console.details %>
  <%# </div> %>
  <%# <div>
    <%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>
  <%# </div> %>
  <%= render 'form_booking', booking: @booking, console: @console %>

  <%= link_to 'New consol', new_console_path %>
  <%= link_to 'Edit consol', edit_console_path(@console) %>
  <%= link_to 'Back', consoles_path %>
  <%= link_to "Delete", console_path(@console),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</div>
