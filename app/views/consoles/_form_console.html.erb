<%= simple_form_for console do |f| %>
  <%= f.input :name %>
  <%= f.input :brand %>
  <%= f.input :price %>
  <%# <%= f.input :details %>
  <%# <%= f.input :img %>
  <%= simple_form_for(console) do |f| %>
  <!-- [...] -->
  <%= f.input :photo, as: :file %>
  <!-- [...] -->
  <%= f.submit class:"btn btn-ghost" %>
<% end %>
